<div>
  <div>
    <%= render 'public/books/index', book: @book, customer: @customer %>
     <div>
       <h2>Books</h2>
       <table>
         <thead>
           <tr>
             <th>タイトル</th>
             <th>本の詳細</th>
             <th colspan="3"></th>
           </tr>
         </thead>
         <tbody>
           <% @books.each do |book| %>
            <tr>
              <td>
                <%= link_to book.customer do %>
                 <%= image_tag book.customer.get_profile_image(40, 40) %>
                <% end %>
              </td>
              <td><%= link_to book.title, public_book_path(book.id) %></td>
              <td><%= book.body %></td>
            </tr>
           <% end %>
         </tbody>
       </table>
     </div>
  </div>
</div>
